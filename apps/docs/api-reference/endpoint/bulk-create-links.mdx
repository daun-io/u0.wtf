---
openapi: post /links/bulk
description: Bulk create up to 100 links for the authenticated project.
---

<Note>
  Bulk link creation is only available on the [Pro plan](https://u0.wtf/pro) and
  above.
</Note>

<Warning>
  Bulk link creation does not support the following features: [Link
  cloaking](https://u0.wtf/help/article/how-to-create-link#link-cloaking),
  [Custom social media
  cards](https://u0.wtf/help/article/how-to-create-link#custom-social-media-cards)
</Warning>

<RequestExample>
```bash cURL
curl --request POST \
  --url https://api.u0.wtf/links/bulk?projectSlug={YOUR_PROJECT_SLUG} \
  --header 'Authorization: Bearer {YOUR_TOKEN}' \
  --header 'Content-Type: application/json' \
  --data '[
    {
      "domain": "yourdomain.com",
      "url": "https://www.youtube.com/"
    },
    {
      "domain": "yourdomain.com",
      "url": "https://www.google.com/"
    }
    ...
  ]'
```
```javascript JavaScript
const options = {
  method: 'POST',
  headers: {Authorization: 'Bearer {YOUR_TOKEN}', 'Content-Type': 'application/json'},
  body: JSON.stringify([
    {
      "domain": "yourdomain.com",
      "url": "https://www.youtube.com/"
    },
    {
      "domain": "yourdomain.com",
      "url": "https://www.google.com/"
    },
    ...
  ])
};

fetch('https://api.u0.wtf/projects/steven/links/bulk', options)
  .then(response => response.json())
  .then(response => console.log(response))
  .catch(err => console.error(err));
```
</RequestExample>